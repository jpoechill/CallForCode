import React from "react";

const HouseDamage = () => {
  return (
    <div id="house-damage-container">
      <h2>HouseDamage</h2>
      <input type="file" name="house_damage_file_upload" accept="image/*" multiple/>
    </div>
  )
};

export default HouseDamage;
